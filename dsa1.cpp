#include <bits/stdc++.h>
using namespace std;

//int i, temp, input;
int n;

int main()
{
	while(cin>>n)
	{
		if(n != 42)
			cout<<n<<endl;
		else
			break;
	}
	return 0;
}
		
/*	do{
		cin >> temp;
		n.push_back(temp);
	} while(cin && cin.peek() != '\n'); */


/*	for(int i=0; i<n.size(); i++)
	{
		if(n[i] != 42)
			cout<<n[i]<<endl;
		else
			break;
	} */

